# 006 — TASK-007 PR #35 CODEX-PR035-01 correction

**Date:** 2026-09-12
**Agent:** Manus
**Authority:** Arko authorization for the narrow correction to existing PR #35 only
**Finding:** CODEX-PR035-01

The following is the attached authorization record preserved verbatim after this metadata header.

mplementation agent: Manus.

AUTHORIZE NARROW CORRECTION TO PR #35 ONLY — CODEX-PR035-01.

Current PR:

#35 — TASK-007 final loop

Do not start a new PR.

Do not merge.

Do not add a completion evaluator.

Do not add revenue-specific logic.

Human product owner / merge authority: Arko.

Independent reviewer: Codex.

FINDING

CODEX-PR035-01

The current implementation correctly gives terminal-state precedence, but the documentation/tests overstate that as generic “already-complete task detection.”

begin_run() creates a RUNNING state and TASK-007 deliberately does not inspect opaque task_state.

Therefore TASK-007 cannot infer:

task_state means this task is already complete

on its own.

REQUIRED CONTRACT CLARIFICATION

Make this explicit:

RunStatus.TASK_COMPLETE is the authoritative precomputed completion signal at TASK-007 loop entry.

TASK-007 does not inspect opaque task state to determine completion.

The upstream task/domain initializer is responsible for determining whether the task is already complete and constructing/returning the appropriate initial state.

For the revenue-agent benchmark, TASK-009 / its initializer will own that domain-specific determination.

Do NOT implement TASK-009 here.

IMPLEMENTATION

Do not add a new completion-evaluator interface.

Do not read task_state.

Preserve the current first check:

if input RunState is terminal, return immediately

This remains correct.

The correction is primarily about:

contract
initialization semantics
tests
closure wording

If a tiny helper or explicit initializer parameter is already naturally supported by current architecture, use it only if necessary. Prefer no new runtime abstraction.

TEST CORRECTION

Replace/rename the misleading “already complete” test so it proves the actual contract:

TASK_COMPLETE entry status causes zero candidate-source calls, zero execution, and zero updater calls.

Add a test proving:

opaque task_state containing something like {\"complete\": True} is not interpreted by TASK-007 when status is RUNNING

This test should make the boundary explicit rather than treating it as a bug.

Do not build completion inference into TASK-007.

TASK-006 CLOSURE CLAIM

Narrow the TASK-006 criterion-10 closure language.

Do not claim:

TASK-007 detects that an arbitrary opaque task is already successful.

Instead state:

TASK-007 demonstrates that an entry state already marked TASK_COMPLETE causes zero candidate evaluation/execution.

The domain-specific responsibility for determining that status lies outside TASK-007.

If criterion 10's current wording requires “task already successful → STOP,” distinguish:

generic loop guarantee: precomputed TASK_COMPLETE state gets zero further work
domain evaluation: owned by the future task-specific initializer/updater

Do not falsely claim a STOP transition is written if the implementation returns the original terminal state unchanged.

Update the relevant TASK-006 wording accordingly.

DOCUMENTATION

Update only the necessary:

tasks/TASK-007_CAPABILITY_RUN_LOOP.md
tasks/TASK-006_GENERALIZED_CAPABILITY_SELECTION.md
PR #35 explanation/review record
focused test names/docs if needed

State explicitly:

TASK_COMPLETE entry status is authoritative. TASK-007 does not infer completion from opaque task_state.

Preserve:

Implementation agent: Manus.

VERIFY

Run:

PYTHONDONTWRITEBYTECODE=1 python3.12 -m unittest discover -q

Confirm:

all existing loop behavior remains unchanged
terminal entry still causes zero source/executor/updater calls
RUNNING state with opaque {\"complete\": True} is not interpreted by the loop
no new evaluator/domain logic added
no provider/integration scope added
REPORT

Push correction to existing PR #35.

Do not merge.

Report:

new head SHA
files changed
final test count
exact contract wording adopted
exact TASK-006 closure claim correction
explicit status of CODEX-PR035-01

End with:

Implementation agent: Manus.

Then stop.

Then give Codex a tiny re-review:

Re-review PR #35 only for CODEX-PR035-01.

Verify:

RunStatus.TASK_COMPLETE is now explicitly the authoritative precomputed completion signal at loop entry
TASK-007 still does not inspect opaque task_state
terminal entry causes zero candidate/executor/updater calls
RUNNING + opaque {\"complete\": True} is intentionally not interpreted
TASK-006 closure wording no longer claims generic completion inference or a STOP transition that does not exist

Do not reopen the already-clean loop mechanics.

If resolved:

No material demo-blocking findings.

Approved

Do not modify code or merge.