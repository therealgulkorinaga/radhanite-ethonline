We’re down to two very contained correctness issues. Fix only these, re-run Codex, then move to live setup.

Use this with Manus:

> **Implementation agent: Manus.**
>
> **AUTHORIZE NARROW CORRECTIONS TO PR #38 ONLY — CODEX-PR038-02 and CODEX-PR038-04.**
>
> Repository:
> `therealgulkorinaga/radhanite-ethonline`
>
> PR:
> `#38 — TASK-015: Circle Wallet + x402 + Arc Testnet Execution`
>
> Current reviewed head:
> `85b758c909053260f20d0905cb2fc0de613d60a7`
>
> Do not create a new PR.
>
> Do not merge.
>
> Do not perform a live Arc payment yet.
>
> Human product owner / merge authority: Arko.
> Independent reviewer: Codex.
>
> Fix only the two remaining findings below.
>
> ---
>
> # CODEX-PR038-02 — REQUIRE COMPLETE AFFIRMATIVE PRE-SIGN ENVELOPE
>
> Current pre-sign classification is still too permissive.
>
> A failure may be treated as safely pre-attempt / zero-spend **only** when the helper returns a complete structured envelope proving no signing or submission occurred.
>
> ## Required correction
>
> The affirmative pre-sign contract must require all of:
>
> * `phase == "pre_sign"`
> * `status == "error"`
> * `signing_started == false`
> * `payment_submitted == false`
> * `commitment_status == "not_committed"`
>
> Missing, contradictory, malformed, or unexpected values must **not** qualify as pre-attempt.
>
> In particular:
>
> ```json
> {
>   "phase": "pre_sign",
>   "status": "error",
>   "signing_started": false,
>   "payment_submitted": false,
>   "commitment_status": "unresolved"
> }
> ```
>
> must produce:
>
> `ArcPaymentCommitmentUnresolvedError`
>
> not:
>
> `ArcPaymentError`
>
> Omitting `commitment_status` must also default to unresolved.
>
> ## Tests
>
> Add:
>
> * complete affirmative `not_committed` envelope -> pre-attempt/no spend
> * `commitment_status: unresolved` -> unresolved
> * missing `commitment_status` -> unresolved
> * contradictory `payment_submitted: true` -> unresolved
> * contradictory `signing_started: true` -> unresolved
> * malformed status/phase -> unresolved unless another authoritative committed state applies
>
> ---
>
> # CODEX-PR038-04 — PRESERVE KNOWN COMMITMENT AFTER SETTLEMENT VALIDATION
>
> Once `PAYMENT-RESPONSE` has been successfully validated, commitment is known.
>
> A later failure while parsing or validating the service body must **not** erase that fact.
>
> ## Required correction
>
> Refactor the Node helper so settlement/payment validation and service-result validation are separate stages.
>
> Required logic:
>
> 1. Parse `PAYMENT-RESPONSE`.
> 2. Validate exact committed amount/reference.
> 3. Persist those validated settlement fields in local state.
> 4. Parse/validate service response.
>
> If service parsing/validation fails **after step 2 succeeds**:
>
> return a structured **committed failure** envelope containing:
>
> * `phase: "post_submit"`
> * commitment status indicating known commitment
> * exact `amount_atomic`
> * authoritative payment/reference
> * service failure reason
> * HTTP status where available
>
> Do not return:
>
> `commitment_status: "unresolved"`
>
> once exact settlement is already established.
>
> Reserve `unresolved` only for cases where authoritative payment acceptance/amount/reference cannot be established.
>
> ## Python behavior
>
> Python must map a committed service failure to the normal TASK-007 failed-execution path with:
>
> * `succeeded = false`
> * exact committed cost
> * authoritative reference/evidence
>
> It must not become:
>
> * zero spend
> * pre-attempt error
> * unresolved commitment
>
> ## Tests
>
> Add:
>
> * settlement validated + malformed JSON service body -> failed receipt with exact committed cost/reference
> * settlement validated + unexpected service schema -> failed receipt with exact committed cost/reference
> * settlement validated + HTTP 500 -> exact committed failure
> * settlement validated + HTTP 404 -> exact committed failure
> * settlement not validated + malformed service/body -> unresolved
> * settlement amount/reference missing -> unresolved
>
> ---
>
> # PRESERVE RESOLVED FINDINGS
>
> Do not regress:
>
> * `CODEX-PR038-01` exact atomic conversion
> * `CODEX-PR038-03` service-result validation/evidence contract
>
> Also preserve:
>
> * no private key
> * Developer-Controlled Wallet
> * `BatchEvmScheme`
> * Arc-only network validation
> * selected-before-sign
> * non-selected candidate never pays
> * no nonce fallback
> * no automatic retry
> * TASK-009 owns state/probability update
>
> ---
>
> # VERIFY
>
> Run:
>
> `PYTHONDONTWRITEBYTECODE=1 python3.12 -m unittest discover -q`
>
> Re-run:
>
> * Arc focused suite
> * Arc + Circle suite
> * pinned Node contract test
>
> Do not run live payment.
>
> ---
>
> # REPORT
>
> Push to existing PR #38.
>
> Do not merge.
>
> Report:
>
> * new head SHA
> * files changed
> * total Python test count
> * Arc test count
> * Arc + Circle test count
> * Node contract result
> * exact affirmative pre-sign envelope
> * exact committed-failure envelope
> * explicit status:
>
>   * `CODEX-PR038-02 resolved`
>   * `CODEX-PR038-04 resolved`
>
> End with:
>
> **Implementation agent: Manus.**
>
> Then stop.

Then give Codex a **very narrow re-review of only 02 and 04**. If both are clean, that’s the point to stop coding and do the human Circle setup + one live Arc smoke.
